name: Build and test SwiftCairo

# Dependencies of this package
env:
  MACOS_BREW: ${{ 'cairo glib glib-networking gobject-introspection pkg-config jq' }}
  UBUNTU_APT: ${{ 'libcairo2-dev libcairo-gobject2 libglib2.0-dev glib-networking gobject-introspection libgirepository1.0-dev libxml2-dev jq' }}

on:
  push:
    branches:
      - development
  pull_request:
    branches:
      - development

jobs:
  # macOS tasks
  build-mac-swift-latest:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '12.2'

      - name: Print Swift version to confirm
        run: swift --version

      - name: Fetch dependencies for general repository
        run: brew install $MACOS_BREW

      - name: Checkout SwiftCairo
        uses: actions/checkout@v2
        with:
          path: SwiftCairo

      - name: Build Swift Wrappers
        run: |
          cd SwiftCairo
          ./run-gir2swift.sh
          echo "GIR2SWIFT_PATH=${PWD}/checkouts/gir2swift/.build/release/gir2swift" >> $GITHUB_ENV
          cd ..

      - name: Build SwiftCairo
        run: |
          cd SwiftCairo
          swift build `./run-gir2swift.sh flags -noUpdate`
          cd ..

      - name: Test SwiftCairo
        run: |
          cd SwiftCairo
          swift test `./run-gir2swift.sh flags -noUpdate`
          cd ..

      - name: Remove unneeded files and archive artifacts
        run: | 
          cd SwiftCairo
          swift package clean
          rm -rf .build/repositories
          cd ..

      - name: 'Upload Artifact'
        uses: actions/upload-artifact@v2
        with:
          name: build-artifact-macos
          path: |
            SwiftCairo/
          retention-days: 1

  # Ubuntu 20.04 tasks
  build-ubuntu-20_04-swift-latest:
    runs-on: ubuntu-20.04
    steps:
      - name: Print Swift version to confirm
        run: swift --version
      - name: Fetch dependencies for general repository
        run: sudo apt-get install $UBUNTU_APT

      - name: Checkout SwiftCairo
        uses: actions/checkout@v2
        with:
          path: SwiftCairo

      - name: Build current SwiftCairo
        run: |
          cd SwiftCairo
          ./run-gir2swift.sh
          echo "GIR2SWIFT_PATH=${PWD}/checkouts/gir2swift/.build/release/gir2swift" >> $GITHUB_ENV
          cd ..

      - name: Build SwiftCairo
        run: |
          cd SwiftCairo
          swift build
          cd ..

      - name: Test SwiftCairo
        run: |
          cd SwiftCairo
          swift test
          cd ..

      - name: Remove unneeded files and archive artifacts
        run: | 
          cd SwiftCairo
          swift package clean
          rm -rf .build/repositories
          cd ..

      - name: 'Upload Artifact'
        uses: actions/upload-artifact@v2
        with:
          name: build-artifact-20.04-5.3
          path: |
            SwiftCairo/
          retention-days: 1

  # Ubuntu 18.04 tasks
  build-ubuntu-18_04-swift-latest:
    runs-on: ubuntu-18.04
    steps:
      - name: Print Swift version to confirm
        run: swift --version
      - name: Fetch dependencies for general repository
        run: sudo apt-get install $UBUNTU_APT

      - name: Checkout SwiftCairo
        uses: actions/checkout@v2
        with:
          path: SwiftCairo

      - name: Build current SwiftCairo
        run: |
          cd SwiftCairo
          ./run-gir2swift.sh
          echo "GIR2SWIFT_PATH=${PWD}/checkouts/gir2swift/.build/release/gir2swift" >> $GITHUB_ENV
          cd ..

      - name: Build SwiftCairo
        run: |
          cd SwiftCairo
          swift build
          cd ..

      - name: Test SwiftCairo
        run: |
          cd SwiftCairo
          swift test
          cd ..

      - name: Remove unneeded files and archive artifacts
        run: |
          cd SwiftCairo
          swift package clean
          rm -rf .build/repositories
          cd ..

      - name: 'Upload Artifact'
        uses: actions/upload-artifact@v2
        with:
          name: build-artifact-18.04-5.3
          path: |
            SwiftCairo/
          retention-days: 1
